# backend

## 环境配置

1. 安装 **Anaconda**;
2. 创建环境`conda create -n SJTUcontest python=3.13.2`;
3. 激活环境`conda activate SJTUcontest`;
4. 安装依赖`pip install -r requirements.txt`;
5. 执行`python manage.py runserver`, 即可在 **8000** 端口上启动后端开发服务器.
